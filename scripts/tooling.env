# Per-app parameters for the shared shell-core release/build scripts (release.sh, gen-latest-json.sh,
# install-app.sh — generated git-ignored by build.rs from the pinned shell-core rev). TRACKED.
APP_NAME=warden
TAURI_CRATE_DIR=crates/warden-app
UPDATER_REPO=lockyc/warden
